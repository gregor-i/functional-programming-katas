package lazy;

public class StreamExercises {

    // FUNCTIONAL PROGRAMMING FORUM

    /*
     * 1. Re-implement `map` and `take` in terms of `unfold`
     */

    /*
     * 2. Implement an infinite stream that returns all natural numbers
     */

    /*
     * 3. Implement an infinite stream that cyclically returns the whole alphabet (upper and lower case, next to each other: aA, bB, cC, dD etc)
     */

    /*
     * 4. Implement an infinite stream that returns the fibonacci numbers
     */

    /*
     * 5. Implement an infinite stream that returns random strings
     */

    /*
     * 6. Implement an infinite stream that returns an infinite solution to the fizz buzz problem
     */
}
